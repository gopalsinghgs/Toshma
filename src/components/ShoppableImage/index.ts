/**
 * @file components/ShoppableImage/index.ts
 * @description Barrel export for the ShoppableImage component module.
 * Only the top-level component and the modal are exported — sub-components
 * (HotspotDot, ProductTooltip) are private implementation details.
 */

export { default as ShoppableImage } from './ShoppableImage';
export { default as ProductDetailModal } from './ProductDetailModal';
